apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cnpg-apps-operator
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/infrastructure/base/cloudnative-pg/operator
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cnpg-apps-cluster
  namespace: flux-system
spec:
  interval: 10m
  dependsOn:
    - name: cnpg-apps-operator
  path: ./kubernetes/infrastructure/base/cloudnative-pg/cluster
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
