apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-cluster-domain
  namespace: traefik
spec:
  secretName: wildcard-cluster-domain-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-cloudflare
  dnsNames:
    - "${cluster_domain}"
    - "*.${cluster_domain}"
