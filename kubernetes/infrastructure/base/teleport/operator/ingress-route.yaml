apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: teleport
  namespace: teleport
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`teleport.${personal_domain}`)
      services:
        - name: teleport
          port: 443
  tls:
    passthrough: true
