apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: teleport
  namespace: teleport
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`teleport.austinsmith.org`)
      services:
        - name: teleport
          port: 443
  tls:
    passthrough: true
