apiVersion: resources.teleport.dev/v2
kind: TeleportUser
metadata:
  name: austin
  namespace: teleport
spec:
  roles:
    - access
    - editor
    - db-access
  traits:
    logins:
      - root
      - ansible
      - austin
