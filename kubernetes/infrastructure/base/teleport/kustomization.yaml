apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: teleport

resources:
  - namespace.yaml
  - helmrelease.yaml
  - helmrepository.yaml
  - pvc-config.yaml
  #- ca-certificate.yaml
  - service.yaml
  - ingressroute.yaml
  - secret.yaml
  #- authentik.yaml Moved to config and added teleport-config.yaml to cluster folder to handle CRD timing
