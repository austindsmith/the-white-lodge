apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: synology-csi
  namespace: synology-csi
spec:
  interval: 30m
  chart:
    spec:
      chart: synology-csi
      version: "0.11.2"
      sourceRef:
        kind: HelmRepository
        name: synology-csi
        namespace: synology-csi
  values:
    clientInfoSecret:
      create: false
      name: synology-csi-client-info
    storageClasses:
      synology-iscsi:
        disabled: false
        reclaimPolicy: Retain
        volumeBindingMode: Immediate
        parameters:
          dsm: "192.168.1.131"
          location: /volume1
          fsType: ext4
          formatOptions: "-E nodiscard -E lazy_itable_init=1,lazy_journal_init=1"
