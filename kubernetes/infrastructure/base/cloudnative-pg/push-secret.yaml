apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: apps-cluster-postgres-cert
  namespace: cnpg-apps
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: op-secret-store
      kind: ClusterSecretStore
  selector:
    secret:
      name: apps-cluster-postgres-cert
  data:
    - match:
        secretKey: tls.crt
        remoteRef:
          remoteKey: apps-cluster-postgres-cert
          property: cert
    - match:
        secretKey: tls.key
        remoteRef:
          remoteKey: apps-cluster-postgres-cert
          property: key
