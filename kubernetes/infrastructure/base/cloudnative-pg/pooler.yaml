apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: apps-cluster-pooler
  namespace: cnpg-apps
spec:
  cluster:
    name: apps-cluster
  instances: 2
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "500"
      default_pool_size: "20"
