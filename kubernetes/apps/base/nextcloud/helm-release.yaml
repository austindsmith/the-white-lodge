apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud
spec:
  install:
    createNamespace: true
  interval: 1h
  chart:
    spec:
      chart: nextcloud
      version: 9.2.2
      sourceRef:
        kind: HelmRepository
        name: nextcloud
  #values:
