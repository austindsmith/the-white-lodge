apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: home-assistant
spec:
  install:
    createNamespace: true
  interval: 1h
  chart:
    spec:
      chart: home-assistant
      version: 0.3.72
      sourceRef:
        kind: HelmRepository
        name: home-assistant
