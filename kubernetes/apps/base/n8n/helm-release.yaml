apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: n8n
spec:
  install:
    createNamespace: true
  interval: 1h
  chart:
    spec:
      chart: n8n
      version: 1.24.10
      sourceRef:
        kind: HelmRepository
        name: n8n
