apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firefox-config
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn # longhorn-backup
  resources:
    requests:
      storage: 5Gi
