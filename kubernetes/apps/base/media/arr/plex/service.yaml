apiVersion: v1
kind: Service
metadata:
  name: plex-lb
  namespace: media
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: plex
    app.kubernetes.io/instance: plex
  ports:
    - name: s3
      port: 32400
      targetPort: 32400
  loadBalancerIP: 192.168.100.202
