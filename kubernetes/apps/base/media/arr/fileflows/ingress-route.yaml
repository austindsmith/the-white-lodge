apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: fileflows
  namespace: media
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`fileflows.${cluster_domain}`)
      kind: Rule
      services:
        - name: fileflows
          port: 80
