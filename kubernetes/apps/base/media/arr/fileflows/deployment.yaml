apiVersion: apps/v1
kind: Deployment
metadata:
  name: fileflows
  namespace: media
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: fileflows
  template:
    metadata:
      labels:
        app: fileflows
    spec:
      containers:
        - name: fileflows
          image: revenz/fileflows
          env:
            - name: PUID
              value: "1037"
            - name: PGID
              value: "100"
            - name: TZ
              value: ${timezone}
          volumeMounts:
            - name: data
              mountPath: /app/Data
            - name: temp
              mountPath: /temp
            - name: media
              mountPath: /app/media
            - name: logs
              mountPath: /app/Logs
            - name: common
              mountPath: /app/common
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: fileflows-data
        - name: temp
          persistentVolumeClaim:
            claimName: fileflows-temp
        - name: media
          persistentVolumeClaim:
            claimName: media
        - name: logs
          persistentVolumeClaim:
            claimName: fileflows-logs
        - name: common
          persistentVolumeClaim:
            claimName: fileflows-common
