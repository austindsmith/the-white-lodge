apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr-ingress
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`radarr.${cluster_domain}`)
      kind: Rule
      services:
        - name: radarr
          port: 7878
