apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gluetun-external-secret
  namespace: media
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: op-secret-store
  target:
    name: gluetun
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: gluetun
