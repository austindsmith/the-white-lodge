apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sabnzbd-ingress
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`sabnzbd.${cluster_domain}`)
      kind: Rule
      services:
        - name: sabnzbd
          port: 8080
