apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: agregarr-settings
spec:
  refreshInterval: 6h
  secretStoreRef:
    kind: ClusterSecretStore
    name: op-secret-store
  target:
    name: agregarr-settings
    creationPolicy: Owner
  data:
    - secretKey: settings.json
      remoteRef:
        key: agregarr/settings.json
