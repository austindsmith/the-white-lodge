apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: gitea
  name: gitea-config
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-backup
  resources:
    requests:
      storage: 2Gi
