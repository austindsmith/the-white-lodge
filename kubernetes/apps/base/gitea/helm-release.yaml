apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gitea
spec:
  install:
    createNamespace: true
  interval: 1h
  chart:
    spec:
      chart: gitea
      version: 12.6.0
      sourceRef:
        kind: HelmRepository
        name: gitea
