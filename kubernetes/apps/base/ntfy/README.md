# Ntfy

Some values in ntfy need to be created with ntfy itself. Create a ntfy container and run the below commands, then add the results to environment variables.

Getting ntfy user hash

```bash
ntfy user hash
```

Getting ntfy keys

```bash
ntfy webpush keys
```
