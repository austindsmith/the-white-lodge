apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: generate

resources:
  - ./namespace.yaml
  - ./helmrelease.yaml
  - ./ingressroute.yaml
  - ./secret.yaml
  - ./seed-cronjob.yaml

configMapGenerator:
  - name: generate-staging-seed-script
    files:
      - seed_generate_staging.py
