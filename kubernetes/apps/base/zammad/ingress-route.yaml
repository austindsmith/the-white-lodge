apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: zammad
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`zammad.${cluster_domain}`)
      kind: Rule
      services:
        - name: zammad
          port: 8080
