apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: data
  name: nessie-config
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-backup
  resources:
    requests:
      storage: 2Gi
