apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nessie
spec:
  install:
    createNamespace: true
  interval: 1h
  chart:
    spec:
      chart: nessie
      version: 0.108.0
      sourceRef:
        kind: HelmRepository
        name: nessie
