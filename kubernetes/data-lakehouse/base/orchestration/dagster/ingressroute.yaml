apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dagster
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`dagster.theblacklodge.dev`)
      kind: Rule
      services:
        - name: dagster-dagster-webserver
          port: 80
