apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dagster
  namespace: data
spec:
  install:
    createNamespace: true
  interval: 1h
  chart:
    spec:
      chart: dagster
      version: 1.13.9
      sourceRef:
        kind: HelmRepository
        name: dagster
  #values:
  #  dagster-user-deployments:
  #    enabled: false
  #    deployments: []
