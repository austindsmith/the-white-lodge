apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: data-cluster-client-cert
  namespace: data
spec:
  updatePolicy: Replace
  deletionPolicy: Delete
  refreshInterval: 1h
  secretStoreRefs:
    - name: op-secret-store
      kind: ClusterSecretStore
  selector:
    secret:
      name: data-cluster-client-cert
  data:
    - match:
        secretKey: ca.crt
        remoteRef:
          remoteKey: data-cluster-client-cert
          property: ca
    - match:
        secretKey: tls.crt
        remoteRef:
          remoteKey: data-cluster-client-cert
          property: cert
    - match:
        secretKey: tls.key
        remoteRef:
          remoteKey: data-cluster-client-cert
          property: key
