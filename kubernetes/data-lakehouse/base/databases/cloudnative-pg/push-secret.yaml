apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: data-cluster-user-cert
  namespace: data
spec:
  updatePolicy: Replace
  deletionPolicy: Delete
  refreshInterval: 6h
  secretStoreRefs:
    - name: op-secret-store
      kind: ClusterSecretStore
  selector:
    secret:
      name: data-cluster-user-cert
  data:
    - match:
        secretKey: ca.crt
        remoteRef:
          remoteKey: data-cluster-user-cert
          property: ca
    - match:
        secretKey: tls.crt
        remoteRef:
          remoteKey: data-cluster-user-cert
          property: cert
    - match:
        secretKey: tls.key
        remoteRef:
          remoteKey: data-cluster-user-cert
          property: key
