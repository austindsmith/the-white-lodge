apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mlflow
spec:
  install:
    createNamespace: true
  interval: 1h
  chart:
    spec:
      chart: mlflow
      version: 1.11.2
      sourceRef:
        kind: HelmRepository
        name: mlflow
