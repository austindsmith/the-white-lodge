apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jupyter-ingress
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`jupyterhub.theblacklodge.dev`)
      kind: Rule
      services:
        - name: proxy-public
          port: 80
