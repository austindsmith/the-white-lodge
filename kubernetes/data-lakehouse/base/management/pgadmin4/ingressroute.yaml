apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pgadmin4
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`pgadmin4.${cluster_domain}`)
      kind: Rule
      services:
        - name: pgadmin4
          port: 80
