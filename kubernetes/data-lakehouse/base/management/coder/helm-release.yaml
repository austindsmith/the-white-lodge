apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: coder
  namespace: data
spec:
  install:
    createNamespace: true
  interval: 1h
  chart:
    spec:
      chart: coder
      version: 2.35.1
      sourceRef:
        kind: HelmRepository
        name: coder
        namespace: data
  #values:
