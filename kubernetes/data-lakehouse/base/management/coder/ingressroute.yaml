apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: coder-ingress
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`coder.theblacklodge.dev`)
      kind: Rule
      services:
        - name: coder
          port: 80
