apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: bytebase
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`bytebase.theblacklodge.dev`)
      kind: Rule
      services:
        - name: bytebase-entrypoint
          port: 80
