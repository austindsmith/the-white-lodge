apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: bytebase
  namespace: data
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`bytebase.${cluster_domain}`)
      kind: Rule
      services:
        - name: bytebase-entrypoint
          port: 80
